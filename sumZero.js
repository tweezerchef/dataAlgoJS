// write a function sumZero which finds the first pair in a sorted array
//and returns them as an array with 0(n) time complexity return "not" if not
//found

function sumZero(numArray){

}

sumZero([-4,-2,-2,-1,0,1,2,5])